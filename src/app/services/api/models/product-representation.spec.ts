import { ProductRepresentation } from './product-representation';

describe('ProductRepresentation', () => {
  it('should create an instance', () => {
    expect(new ProductRepresentation()).toBeTruthy();
  });
});
