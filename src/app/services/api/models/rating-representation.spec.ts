import { RatingRepresentation } from './rating-representation';

describe('RatingRepresentation', () => {
  it('should create an instance', () => {
    expect(new RatingRepresentation()).toBeTruthy();
  });
});
